[pytest]
markers =
    integration: integration-style coverage for service/model flows that are broader than pure unit tests
filterwarnings =
    ignore:datetime.datetime.utcnow\(\) is deprecated and scheduled for removal.*:DeprecationWarning:jose\.jwt
