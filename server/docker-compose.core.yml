# Shared application stack (no Postgres). Combine with exactly one of:
#   docker-compose.data-prod.yml  OR  docker-compose.data-mock.yml
# Entry points: docker-compose.yml (prod DB) or docker-compose.sim.yml (mock/sim DB).

services:
  mosquitto:
    image: eclipse-mosquitto:2
    restart: unless-stopped
    ports:
      - "1883:1883"
    volumes:
      - ./mosquitto.conf:/mosquitto/config/mosquitto.conf:ro
      - mosquitto_data:/mosquitto/data
      - mosquitto_log:/mosquitto/log
    healthcheck:
      test: ["CMD", "mosquitto_pub", "-h", "localhost", "-t", "health", "-m", "ok", "-q", "0"]
      interval: 10s
      timeout: 5s
      retries: 3

  wheelsense-platform-server:
    container_name: wheelsense-platform-server
    labels:
      org.opencontainers.image.title: "Ease AI Server"
      org.opencontainers.image.description: "Ease AI platform API and device services"
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      APP_NAME: ${APP_NAME:-Ease AI Server}
      SECRET_KEY: ${SECRET_KEY:-wheelsense-local-dev-secret-key}
      MQTT_BROKER: ${MQTT_BROKER:-mosquitto}
      MQTT_PORT: ${MQTT_PORT:-1883}
      MQTT_USER: ${MQTT_USER:-}
      MQTT_PASSWORD: ${MQTT_PASSWORD:-}
      AI_PROVIDER: ${AI_PROVIDER:-copilot}
      AI_DEFAULT_MODEL: ${AI_DEFAULT_MODEL:-gpt-4.1}
      OLLAMA_BASE_URL: ${OLLAMA_BASE_URL:-http://host.docker.internal:11434/v1}
      OLLAMA_FALLBACK_MODEL: ${OLLAMA_FALLBACK_MODEL:-gemma4:e4b}
      COPILOT_CLI_URL: ${COPILOT_CLI_URL:-copilot-cli:4321}
      COPILOT_GITHUB_TOKEN: ${COPILOT_GITHUB_TOKEN:-}
      INTENT_SEMANTIC_ENABLED: ${INTENT_SEMANTIC_ENABLED:-false}
      INTENT_LLM_NORMALIZE_ENABLED: ${INTENT_LLM_NORMALIZE_ENABLED:-false}
      SERVER_BASE_URL: ${SERVER_BASE_URL:-http://wheelsense-platform-server:8000}
      PORTAL_BASE_URL: ${PORTAL_BASE_URL:-}
      AGENT_RUNTIME_URL: ${AGENT_RUNTIME_URL:-http://wheelsense-agent-runtime:8010}
      INTERNAL_SERVICE_SECRET: ${INTERNAL_SERVICE_SECRET:-wheelsense-internal-dev-secret}
      MCP_ALLOWED_ORIGINS: ${MCP_ALLOWED_ORIGINS:-http://localhost:3000}
      GITHUB_OAUTH_CLIENT_ID: ${GITHUB_OAUTH_CLIENT_ID:-}
      BOOTSTRAP_ADMIN_ENABLED: ${BOOTSTRAP_ADMIN_ENABLED:-true}
      BOOTSTRAP_ADMIN_USERNAME: ${BOOTSTRAP_ADMIN_USERNAME:-admin}
      BOOTSTRAP_ADMIN_PASSWORD: ${BOOTSTRAP_ADMIN_PASSWORD:-wheelsense2026}
      BOOTSTRAP_ADMIN_SYNC_PASSWORD: "true"
      BOOTSTRAP_ADMIN_ATTACH_DEMO_WORKSPACE: "true"
      PROFILE_IMAGE_STORAGE_DIR: /app/storage/profile_images
    volumes:
      - profile_images:/app/storage/profile_images
    depends_on:
      db:
        condition: service_healthy
      mosquitto:
        condition: service_healthy

  wheelsense-agent-runtime:
    container_name: wheelsense-agent-runtime
    labels:
      org.opencontainers.image.title: "Ease AI Agent Runtime"
      org.opencontainers.image.description: "Ease AI constrained assistant runtime"
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    command: ["uvicorn", "app.agent_runtime.main:app", "--host", "0.0.0.0", "--port", "8010"]
    ports:
      - "8010:8010"
    environment:
      SECRET_KEY: ${SECRET_KEY:-wheelsense-local-dev-secret-key}
      DATABASE_URL: postgresql+asyncpg://wheelsense:${POSTGRES_PASSWORD:-wheelsense_dev}@db:5432/wheelsense
      DATABASE_URL_SYNC: postgresql://wheelsense:${POSTGRES_PASSWORD:-wheelsense_dev}@db:5432/wheelsense
      SERVER_BASE_URL: ${SERVER_BASE_URL:-http://wheelsense-platform-server:8000}
      # Call back into the platform MCP Streamable HTTP endpoint (see FastMCP streamable_http_path).
      AGENT_RUNTIME_MCP_TOOL_TRANSPORT: ${AGENT_RUNTIME_MCP_TOOL_TRANSPORT:-http}
      MCP_STREAMABLE_HTTP_URL: ${MCP_STREAMABLE_HTTP_URL:-http://wheelsense-platform-server:8000/mcp/mcp}
      MCP_STREAMABLE_HTTP_HOST_HEADER: ${MCP_STREAMABLE_HTTP_HOST_HEADER:-localhost:8000}
      AGENT_RUNTIME_URL: ${AGENT_RUNTIME_URL:-http://wheelsense-agent-runtime:8010}
      INTERNAL_SERVICE_SECRET: ${INTERNAL_SERVICE_SECRET:-wheelsense-internal-dev-secret}
      AI_PROVIDER: ${AI_PROVIDER:-copilot}
      AI_DEFAULT_MODEL: ${AI_DEFAULT_MODEL:-gpt-4.1}
      OLLAMA_BASE_URL: ${OLLAMA_BASE_URL:-http://host.docker.internal:11434/v1}
      OLLAMA_FALLBACK_MODEL: ${OLLAMA_FALLBACK_MODEL:-gemma4:e4b}
      COPILOT_CLI_URL: ${COPILOT_CLI_URL:-copilot-cli:4321}
      COPILOT_GITHUB_TOKEN: ${COPILOT_GITHUB_TOKEN:-}
      INTENT_SEMANTIC_ENABLED: ${INTENT_SEMANTIC_ENABLED:-false}
      INTENT_LLM_NORMALIZE_ENABLED: ${INTENT_LLM_NORMALIZE_ENABLED:-false}
      # Agent routing: `intent` (classifier) or `llm_tools` (provider-assisted MCP tool routing).
      EASEAI_PIPELINE_V2: ${EASEAI_PIPELINE_V2:-true}
      AGENT_ROUTING_MODE: ${AGENT_ROUTING_MODE:-llm_tools}
      AGENT_LLM_ROUTER_MODEL: ${AGENT_LLM_ROUTER_MODEL:-}
      # AI-primary mode: bypass deterministic answer locks so the ADR 0015 LLM
      # tool router runs as the primary decision-maker. Only page-context and
      # identity fast paths remain. Set `true` to restore legacy locked answers.
      EASEAI_DETERMINISTIC_ANSWER_LOCK_ENABLED: ${EASEAI_DETERMINISTIC_ANSWER_LOCK_ENABLED:-false}
    depends_on:
      wheelsense-platform-server:
        condition: service_started

  wheelsense-platform-web:
    container_name: wheelsense-platform-web
    labels:
      org.opencontainers.image.title: "Ease AI Web"
      org.opencontainers.image.description: "Ease AI clinical operations web interface"
    build:
      context: ../frontend
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      WHEELSENSE_API_ORIGIN: http://wheelsense-platform-server:8000
    depends_on:
      wheelsense-platform-server:
        condition: service_started
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:3000/login').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 20s
    develop:
      watch:
        - action: rebuild
          path: ../frontend
          ignore:
            - node_modules/
            - .next/
            - "**/.git/"

  copilot-cli:
    profiles:
      - copilot
    image: ghcr.io/github/copilot-cli:latest
    restart: unless-stopped
    command: ["--headless", "--port", "4321"]
    ports:
      - "4321:4321"
    environment:
      - COPILOT_GITHUB_TOKEN=${COPILOT_GITHUB_TOKEN:-}
    volumes:
      - copilot_sessions:/root/.copilot/session-state

  homeassistant:
    image: ghcr.io/home-assistant/home-assistant:stable
    restart: unless-stopped
    ports:
      - "8123:8123"
    volumes:
      - ./homeassistant:/config
    environment:
      - TZ=Asia/Bangkok
    network_mode: bridge

volumes:
  profile_images:
  mosquitto_data:
  mosquitto_log:
  ollama_data:
  copilot_sessions:
