# Shared application stack (no Postgres). Combine with exactly one of:
#   docker-compose.data-prod.yml  OR  docker-compose.data-mock.yml
# Entry points: docker-compose.yml (prod DB) or docker-compose.sim.yml (mock/sim DB).

services:
  mosquitto:
    image: eclipse-mosquitto:2
    restart: unless-stopped
    ports:
      - "1883:1883"
    volumes:
      - ./mosquitto.conf:/mosquitto/config/mosquitto.conf:ro
      - mosquitto_data:/mosquitto/data
      - mosquitto_log:/mosquitto/log
    healthcheck:
      test: ["CMD", "mosquitto_pub", "-h", "localhost", "-t", "health", "-m", "ok", "-q", "0"]
      interval: 10s
      timeout: 5s
      retries: 3

  wheelsense-platform-server:
    container_name: wheelsense-platform-server
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      SECRET_KEY: ${SECRET_KEY:-wheelsense-local-dev-secret-key}
      MQTT_BROKER: ${MQTT_BROKER:-mosquitto}
      MQTT_PORT: ${MQTT_PORT:-1883}
      MQTT_USER: ${MQTT_USER:-}
      MQTT_PASSWORD: ${MQTT_PASSWORD:-}
      AI_PROVIDER: ${AI_PROVIDER:-ollama}
      AI_DEFAULT_MODEL: ${AI_DEFAULT_MODEL:-gemma4:e4b}
      OLLAMA_BASE_URL: ${OLLAMA_BASE_URL:-http://host.docker.internal:11434/v1}
      COPILOT_CLI_URL: ${COPILOT_CLI_URL:-copilot-cli:4321}
      COPILOT_GITHUB_TOKEN: ${COPILOT_GITHUB_TOKEN:-}
      GITHUB_OAUTH_CLIENT_ID: ${GITHUB_OAUTH_CLIENT_ID:-}
      BOOTSTRAP_ADMIN_ENABLED: ${BOOTSTRAP_ADMIN_ENABLED:-true}
      BOOTSTRAP_ADMIN_USERNAME: ${BOOTSTRAP_ADMIN_USERNAME:-admin}
      BOOTSTRAP_ADMIN_PASSWORD: ${BOOTSTRAP_ADMIN_PASSWORD:-wheelsense2026}
      BOOTSTRAP_ADMIN_SYNC_PASSWORD: "true"
      BOOTSTRAP_ADMIN_ATTACH_DEMO_WORKSPACE: "true"
      PROFILE_IMAGE_STORAGE_DIR: /app/storage/profile_images
    volumes:
      - profile_images:/app/storage/profile_images
    depends_on:
      db:
        condition: service_healthy
      mosquitto:
        condition: service_healthy

  wheelsense-platform-web:
    container_name: wheelsense-platform-web
    build:
      context: ../frontend
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      WHEELSENSE_API_ORIGIN: http://wheelsense-platform-server:8000
    depends_on:
      wheelsense-platform-server:
        condition: service_started
    develop:
      watch:
        - action: rebuild
          path: ../frontend
          ignore:
            - node_modules/
            - .next/
            - "**/.git/"

  copilot-cli:
    profiles:
      - copilot
    image: ghcr.io/github/copilot-cli:latest
    restart: unless-stopped
    command: ["--headless", "--port", "4321"]
    ports:
      - "4321:4321"
    environment:
      - COPILOT_GITHUB_TOKEN=${COPILOT_GITHUB_TOKEN:-}
    volumes:
      - copilot_sessions:/root/.copilot/session-state

  homeassistant:
    image: ghcr.io/home-assistant/home-assistant:stable
    restart: unless-stopped
    ports:
      - "8123:8123"
    volumes:
      - ./homeassistant:/config
    environment:
      - TZ=Asia/Bangkok
    network_mode: bridge

volumes:
  profile_images:
  mosquitto_data:
  mosquitto_log:
  ollama_data:
  copilot_sessions:
