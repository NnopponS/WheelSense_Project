import StaffMonitoringPage from "@/components/floorplan/StaffMonitoringPage";

export default function ObserverMonitoringPage() {
  return (
    <StaffMonitoringPage
      title="Observer Monitoring"
      subtitle="Watch active rooms, confirm who is present, and inspect connected devices and room snapshots from the live monitoring map."
    />
  );
}
