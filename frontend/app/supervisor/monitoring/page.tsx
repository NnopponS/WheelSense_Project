import StaffMonitoringPage from "@/components/floorplan/StaffMonitoringPage";

export default function SupervisorMonitoringPage() {
  return (
    <StaffMonitoringPage
      title="Supervisor Monitoring"
      subtitle="Use the live ward map to review who is in each room, inspect alert pressure, and open the latest room snapshot without leaving the command workflow."
    />
  );
}
