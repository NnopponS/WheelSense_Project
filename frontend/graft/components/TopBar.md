# components/TopBar.tsx

- SimulatorStatus · type · L24-L28 — type SimulatorStatus = { env_mode: string; is_simulator: boolean; workspace_exists: boolean; };
- TopBarProps · interface · L30-L34 — interface TopBarProps
- TopBar · function · L44-L220 — function TopBar({ title, subtitle, onMenuClick }: TopBarProps)
