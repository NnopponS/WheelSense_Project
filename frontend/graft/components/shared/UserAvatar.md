# components/shared/UserAvatar.tsx

- UserAvatarProps · interface · L5-L12 — interface UserAvatarProps
- initialsFromUsername · function · L14-L22 — function initialsFromUsername(username: string): string
- UserAvatar · function · L27-L67 — function UserAvatar({ username, profileImageUrl, sizePx = 32, className = "", fallbackClassName = "gradient-cta text-white", }: UserAvatarProps)
